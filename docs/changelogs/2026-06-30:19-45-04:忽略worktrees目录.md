# 忽略 .worktrees/ 目录

## 变更摘要
新增 `.gitignore`，忽略 `.worktrees/` 目录。

## 动机 / 背景
开始实现「聊天历史搜索（Elasticsearch）」功能前，使用 git worktree 创建隔离工作区，
worktree 目录置于项目内 `.worktrees/`，需确保其内容不被误提交到仓库。

## 涉及文件
- 新增 `.gitignore`
