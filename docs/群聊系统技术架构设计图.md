下面给你一套**群聊系统技术架构设计图**，重点围绕：

```text
群聊
WebSocket 长连接
大群消息广播
消息可靠性
Redis 在线状态
Kafka 异步投递
MySQL 持久化
大群性能优化
```

------

# 1. 总体技术架构图

```mermaid
flowchart TB
    Client[前端 App<br/>群列表 / 群聊窗口 / 群设置 / 成员管理]

    subgraph Edge[接入层]
        Nginx[Nginx / API Gateway<br/>HTTPS / WebSocket 入口<br/>鉴权 / 限流 / 路由]
    end

    subgraph Backend[后端服务层]
        API[HTTP API 服务<br/>群管理 / 成员管理 / 历史消息查询]
        WS[WebSocket Gateway<br/>长连接管理 / 心跳 / 消息收发]
        GroupSvc[群服务 Group Service<br/>群资料 / 群成员 / 权限 / 禁言]
        MsgSvc[消息服务 Message Service<br/>消息校验 / Sequence / 落库 / ACK]
        Delivery[投递服务 Delivery Service<br/>消费队列 / 大群分片广播 / 推送]
    end

    subgraph MQ[消息队列层]
        Kafka[Kafka<br/>群消息投递队列<br/>削峰 / 异步广播]
    end

    subgraph Storage[存储层]
        MySQL[(MySQL<br/>群 / 成员 / 消息 / 公告 / 审批)]
        Redis[(Redis<br/>在线状态 / 连接路由 / Sequence / 限流 / 缓存)]
    end

    subgraph Observability[可观测性]
        Prom[Prometheus<br/>指标采集]
        Grafana[Grafana<br/>监控大盘]
        Log[日志系统<br/>结构化日志 / TraceId]
    end

    Client -->|HTTP / WebSocket| Nginx

    Nginx -->|HTTP| API
    Nginx -->|WebSocket| WS

    API --> GroupSvc
    API --> MsgSvc
    WS --> MsgSvc

    GroupSvc --> MySQL
    GroupSvc --> Redis

    MsgSvc --> Redis
    MsgSvc --> MySQL
    MsgSvc --> Kafka

    Kafka --> Delivery
    Delivery --> Redis
    Delivery --> WS

    API --> Prom
    WS --> Prom
    MsgSvc --> Prom
    Delivery --> Prom

    Prom --> Grafana
    API --> Log
    WS --> Log
    MsgSvc --> Log
    Delivery --> Log
```

------

# 2. 架构分层说明

## 2.1 前端层

负责群聊交互：

```text
群列表
群聊消息窗口
消息输入框
群成员列表
群设置
群管理中心
消息搜索
群公告
群文件
大群性能提示
```

前端重点能力：

```text
WebSocket 自动重连
心跳保活
消息 ACK 等待
发送失败重试
消息本地临时展示
历史消息分页
虚拟列表渲染
未读数展示
@我提醒
```

------

## 2.2 接入层

接入层统一处理：

```text
HTTP 请求
WebSocket 连接
鉴权
限流
负载均衡
跨域
TLS
```

可以使用：

```text
Nginx
API Gateway
Kong
Traefik
```

学习阶段直接用 Nginx 即可。

------

## 2.3 WebSocket Gateway

WebSocket Gateway 是群聊系统的核心入口。

负责：

```text
用户连接建立
Token 鉴权
连接注册
心跳检测
断线清理
接收客户端消息
向客户端推送消息
```

内部维护本机连接：

```text
connectionId -> WebSocket连接
userId -> connectionId列表
```

同时把全局连接状态写入 Redis：

```text
online:user:{userId} -> serverId
server:{serverId}:users -> userId集合
```

------

## 2.4 群服务 Group Service

群服务负责群管理能力：

```text
创建群
修改群信息
群成员分页
加群审批
踢人
退群
设置管理员
群公告
禁言
解散群
大群模式配置
慢速模式配置
```

群服务主要读写：

```text
chat_group
group_member
group_join_request
group_announcement
```

------

## 2.5 消息服务 Message Service

消息服务负责消息发送的核心逻辑：

```text
校验用户是否在群内
校验群状态
校验用户是否禁言
校验慢速模式
生成 messageId
生成 group sequence
消息落库
返回 ACK
写入 Kafka
```

消息服务遵循原则：

```text
先校验
再生成 sequence
再落库
再 ACK
再异步投递
```

------

## 2.6 投递服务 Delivery Service

投递服务负责把消息真正推给在线成员。

职责：

```text
消费 Kafka 消息
查询群成员
过滤在线成员
按 WebSocket 节点分组
批量推送
失败重试
记录投递指标
```

大群时不能同步逐个推送，必须异步分片。

------

## 2.7 Redis

Redis 用于高频状态和缓存：

```text
用户在线状态
WebSocket 连接路由
群最大 sequence
群配置缓存
群成员缓存
限流计数器
慢速模式控制
@所有人频率控制
```

典型 Key：

```text
online:user:{userId}
server:{serverId}:users
group:{groupId}:sequence
group:{groupId}:config
group:{groupId}:max_sequence
rate_limit:group:{groupId}:user:{userId}
rate_limit:group:{groupId}:mention_all
```

------

## 2.8 MySQL

MySQL 存储核心业务数据：

```text
群信息
群成员
群消息
群公告
加群申请
禁言记录
撤回记录
```

核心表：

```text
chat_group
group_member
group_message
group_announcement
group_join_request
group_mute_record
```

------

## 2.9 Kafka

Kafka 用于消息异步投递：

```text
群消息投递
系统消息投递
@提醒投递
大群广播削峰
后续消息搜索索引构建
```

建议 Topic：

```text
group-message-topic
group-system-event-topic
group-mention-topic
```

------

# 3. 群消息发送链路图

```mermaid
sequenceDiagram
    participant C as 前端客户端
    participant WS as WebSocket Gateway
    participant MSG as 消息服务
    participant Redis as Redis
    participant DB as MySQL
    participant MQ as Kafka
    participant D as 投递服务
    participant Other as 其他在线成员

    C->>WS: group_message_send<br/>携带 clientMessageId
    WS->>MSG: 转发发送请求

    MSG->>DB: 校验群成员关系
    MSG->>DB: 校验群状态 / 禁言状态
    MSG->>Redis: 校验慢速模式 / 限流
    MSG->>Redis: INCR group:{groupId}:sequence
    MSG->>DB: 写入 group_message
    MSG-->>WS: 返回 ACK<br/>messageId + sequence
    WS-->>C: group_message_ack

    MSG->>MQ: 写入 group-message-topic

    MQ-->>D: 消费群消息
    D->>Redis: 查询在线用户 / 连接路由
    D->>D: 按 WebSocket 节点分组
    D->>WS: 批量投递给对应节点
    WS-->>Other: group_message_receive
```

------

# 4. 大群消息广播架构图

```mermaid
flowchart LR
    UserA[发送者] --> WS1[WebSocket Gateway-1]
    WS1 --> MsgSvc[消息服务<br/>校验 / 落库 / ACK]
    MsgSvc --> DB[(MySQL<br/>消息持久化)]
    MsgSvc --> Redis[(Redis<br/>Sequence / 限流 / 群配置)]
    MsgSvc --> Kafka[Kafka<br/>群消息队列]

    Kafka --> Delivery[投递服务]

    Delivery --> RedisRoute[(Redis<br/>在线用户连接路由)]

    Delivery --> Task1[投递任务<br/>server-1 用户集合]
    Delivery --> Task2[投递任务<br/>server-2 用户集合]
    Delivery --> Task3[投递任务<br/>server-3 用户集合]
    Delivery --> Task4[投递任务<br/>server-4 用户集合]

    Task1 --> WSNode1[WebSocket Gateway-1]
    Task2 --> WSNode2[WebSocket Gateway-2]
    Task3 --> WSNode3[WebSocket Gateway-3]
    Task4 --> WSNode4[WebSocket Gateway-4]

    WSNode1 --> U1[在线成员 A/B/C...]
    WSNode2 --> U2[在线成员 D/E/F...]
    WSNode3 --> U3[在线成员 G/H/I...]
    WSNode4 --> U4[在线成员 J/K/L...]
```

核心思想：

```text
消息服务不直接广播大群消息。
消息服务只负责校验、落库、ACK、写队列。
真正的广播由投递服务异步完成。
投递服务按 WebSocket 节点分片推送。
```

------

# 5. WebSocket 多节点连接路由图

```mermaid
flowchart TB
    subgraph Clients[客户端]
        U1[用户 1001]
        U2[用户 1002]
        U3[用户 1003]
        U4[用户 1004]
    end

    subgraph WSLayer[WebSocket Gateway 集群]
        WS1[WS-Server-1]
        WS2[WS-Server-2]
        WS3[WS-Server-3]
    end

    Redis[(Redis<br/>全局连接路由)]

    U1 --> WS1
    U2 --> WS1
    U3 --> WS2
    U4 --> WS3

    WS1 --> Redis
    WS2 --> Redis
    WS3 --> Redis

    Redis --> R1[online:user:1001 -> WS-Server-1]
    Redis --> R2[online:user:1002 -> WS-Server-1]
    Redis --> R3[online:user:1003 -> WS-Server-2]
    Redis --> R4[online:user:1004 -> WS-Server-3]
```

当投递服务要推送消息给用户时：

```text
1. 先查 Redis，知道用户在哪台 WebSocket 节点上。
2. 按 serverId 聚合用户。
3. 把消息发给对应 WebSocket 节点。
4. WebSocket 节点只负责推送自己本机连接。
```

------

# 6. 消息可靠性设计图

```mermaid
stateDiagram-v2
    [*] --> Sending: 用户点击发送

    Sending --> Success: 收到服务端 ACK
    Sending --> Failed: ACK 超时 / 网络异常 / 服务端拒绝

    Failed --> Sending: 用户点击重试

    Success --> Delivered: 服务端异步投递
    Delivered --> Read: 用户阅读后上报 lastReadSequence

    Success --> Recalled: 用户或管理员撤回
    Failed --> [*]
    Read --> [*]
    Recalled --> [*]
```

消息可靠性依赖三个关键设计：

```text
clientMessageId：防止重复发送
server ACK：确认服务端已成功处理
sequence：保证群内消息顺序
```

------

# 7. 存储架构图

```mermaid
flowchart TB
    subgraph MySQL["MySQL 业务数据"]
        G["chat_group<br/>群信息"]
        M["group_member<br/>群成员 / 角色 / lastReadSequence"]
        MSG["group_message<br/>群消息 / sequence / 状态"]
        A["group_announcement<br/>群公告"]
        J["group_join_request<br/>加群申请"]
        MR["group_mute_record<br/>禁言记录"]
    end

    subgraph Redis["Redis 高频状态"]
        R1["group:{groupId}:sequence<br/>群消息序号"]
        R2["group:{groupId}:config<br/>群配置缓存"]
        R3["online:user:{userId}<br/>用户连接路由"]
        R4["server:{serverId}:users<br/>节点在线用户"]
        R5["rate_limit:*<br/>限流 / 慢速模式"]
        R6["group:{groupId}:max_sequence<br/>群最大消息序号"]
    end

    subgraph Kafka["Kafka 消息队列"]
        K1["group-message-topic<br/>群消息投递"]
        K2["group-system-event-topic<br/>系统事件"]
        K3["group-mention-topic<br/>@提醒"]
    end

    MSG --> R1
    MSG --> R6
    MSG --> K1

    G --> R2
    M --> R3
    M --> R4
    M --> R5

    A --> K2
    J --> K2
    MSG --> K3
```

------

# 8. 未读数设计图

```mermaid
flowchart LR
    MsgMax[群最大消息序号<br/>group_max_sequence = 100260]
    UserRead[用户读取位置<br/>lastReadSequence = 100201]

    MsgMax --> Calc[未读数计算]
    UserRead --> Calc

    Calc --> Result[unreadCount = 100260 - 100201 = 59]
```

设计原则：

```text
不为每个用户、每条消息都写已读记录。
每个用户在每个群只维护一个 lastReadSequence。
未读数通过群最大 sequence 减去用户 lastReadSequence 得到。
```

这样适合大群。

------

# 9. 大群性能优化架构图

```mermaid
flowchart TB
    LargeGroup["大群场景<br/>1万 - 10万人"]

    subgraph L1["第一层：产品与交互约束"]
        P1["消息只存一份"]
        P2["离线用户上线后补拉"]
        P3["不展示完整已读名单"]
        P4["成员列表分页"]
        P5["历史消息游标分页"]
        P6["@所有人限频"]
        P7["慢速模式"]
        P8["虚拟列表渲染"]
    end

    subgraph L2["第二层：后端架构优化"]
        B1["Kafka 异步投递"]
        B2["WebSocket 节点分片推送"]
        B3["只推送在线用户"]
        B4["热点群保护"]
    end

    subgraph L3["第三层：存储与缓存支撑"]
        S1["MySQL<br/>groupId + sequence 索引"]
        S2["Redis<br/>限流计数"]
        S3["Redis<br/>连接路由"]
        S4["Redis<br/>群配置缓存"]
    end

    LargeGroup --> L1
    L1 --> L2
    L2 --> L3

    P4 -.-> S1
    P5 -.-> S1
    P6 -.-> S2
    P7 -.-> S2

    B1 --> B2
    B2 --> S3
    B3 --> S3
    B4 --> S2
    B4 --> S4
```

------

# 10. 推荐最终技术架构总结图

```mermaid
flowchart TB
    Client[React / Vue 前端<br/>群聊页面 / 虚拟列表 / WebSocket 客户端]

    Gateway[Nginx / API Gateway<br/>统一入口 / 鉴权 / 限流]

    subgraph App[应用服务]
        WS[WebSocket Gateway<br/>连接管理 / 心跳 / 推送]
        API[HTTP API<br/>群管理 / 成员 / 历史消息]
        Group[Group Service<br/>群资料 / 成员 / 权限]
        Message[Message Service<br/>发送校验 / Sequence / 落库 / ACK]
        Delivery[Delivery Service<br/>异步广播 / 分片推送]
    end

    subgraph Middleware[中间件]
        Redis[(Redis<br/>在线状态 / Sequence / 缓存 / 限流)]
        Kafka[(Kafka<br/>消息队列 / 削峰 / 异步投递)]
    end

    subgraph DB[持久化]
        MySQL[(MySQL<br/>群 / 成员 / 消息 / 公告)]
    end

    subgraph Monitor[监控]
        Prometheus[Prometheus]
        Grafana[Grafana]
        Logs[结构化日志]
    end

    Client --> Gateway
    Gateway --> WS
    Gateway --> API

    WS --> Message
    API --> Group
    API --> Message

    Group --> MySQL
    Group --> Redis

    Message --> Redis
    Message --> MySQL
    Message --> Kafka

    Kafka --> Delivery
    Delivery --> Redis
    Delivery --> WS

    WS --> Prometheus
    API --> Prometheus
    Message --> Prometheus
    Delivery --> Prometheus

    Prometheus --> Grafana

    WS --> Logs
    API --> Logs
    Message --> Logs
    Delivery --> Logs
```

------

# 11. 一句话说明这张架构图

这套架构的核心是：

```text
前端通过 WebSocket 发送和接收群消息；
WebSocket Gateway 负责长连接；
消息服务负责校验、生成 sequence、落库和 ACK；
Kafka 负责大群消息削峰；
投递服务负责异步分片广播；
Redis 负责在线状态、连接路由、限流和缓存；
MySQL 负责群、成员、消息等核心数据持久化。
```

项目初期即使不马上拆成多个微服务，也建议**代码结构按这个架构拆模块**，这样后续做大群、多节点、压测和扩展时，不需要推翻重做。